Steps of preparing GPM data:

1. make_remap_weight_file.py - make remapping weight file from GPM to SMAP grid

2. process_from_raw_GPM.remap.py - process the raw downloaded GPM files to SMAP grid; output 30min, 36-km GPM files, one file per 30 min

3. aggregate_GPM_12hour.py - aggregate 30 min, 36 kim GPM to 12-hour (SMAP resolution), 36 km

